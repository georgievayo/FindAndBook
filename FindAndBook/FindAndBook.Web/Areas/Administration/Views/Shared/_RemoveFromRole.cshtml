@model string
@Ajax.ActionLink("Remove admin", "RemoveAdmin", new { controller="Administration", id = @Model },
    new AjaxOptions
    {
        UpdateTargetId = "action-" + @Model,
        InsertionMode = InsertionMode.Replace
    })
